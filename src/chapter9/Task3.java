package chapter9;

public class Task3 {
    // (a>90 && a<100)
    // ((a+b)>100) && ((a+b)<200)
    // (a%3 == 0) && (a%4 == 0)
    // (a%2 == 0) && (a%10 != 0)
    // (a%3 == 0) && (a%4 != 0)
    // (a%5 == 2) && (a%3 == 1)
    // (a/10 == 3) || (a%10 == 3)
    // (a >= 1) && (a <= 5)
    // (math >= 4) && (eng >= 4)
}
